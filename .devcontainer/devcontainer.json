{
  "name": "pulseWebKit",
  "dockerComposeFile": "docker-compose.yml",
  "service": "web",
  "workspaceFolder": "/workspaces/${localWorkspaceFolderBasename}",
  "shutdownAction": "stopCompose",
  "remoteUser": "node",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {},
    "ghcr.io/devcontainers/features/github-cli:1": {},
    "ghcr.io/anthropics/devcontainer-features/claude-code:1.0": {}
  },
  "mounts": [
    "source=claude-code-config-${devcontainerId},target=/home/node/.claude,type=volume"
  ],
  "containerEnv": {
    "CLAUDE_CONFIG_DIR": "/home/node/.claude",
    "ANTHROPIC_API_KEY": "${localEnv:ANTHROPIC_API_KEY}"
  },
  "forwardPorts": [5173, 8001],
  "portsAttributes": {
    "5173": { "label": "Web app (dev server)", "onAutoForward": "notify" },
    "8001": { "label": "Mock backend (FastAPI)", "onAutoForward": "silent" }
  },
  "postCreateCommand": "bash .devcontainer/post-create.sh",
  "postStartCommand": "bash .devcontainer/post-start.sh",
  "postAttachCommand": {
    "hint": "echo 'Web app lives in src/your-web (scaffold it, then `cd src/your-web && npm run dev`).' && echo 'Claude Code: run `claude` to verify auth (first use triggers browser login; subsequent rebuilds reuse the persisted config volume).'",
    "claude-auth-check": "claude config list > /dev/null 2>&1 && echo 'Claude: authenticated.' || echo 'Claude: not authenticated — run `claude` to log in.'"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "anthropic.claude-code",
        "esbenp.prettier-vscode",
        "ms-playwright.playwright",
        "eamodio.gitlens",
        "ms-azuretools.vscode-docker",
        "redhat.vscode-yaml"
      ],
      "settings": {
        "task.allowAutomaticTasks": "on"
      }
    }
  }
}
